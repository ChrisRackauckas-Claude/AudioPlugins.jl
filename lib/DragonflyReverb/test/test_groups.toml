# `os` is root-matrix-only upstream; a sublibrary OS axis needs one section per OS.
[Core]
versions = ["lts", "1"]
runner = "ubuntu-latest"

[Core-macOS]
versions = ["lts", "1"]
runner = "macos-latest"

[Core-Windows]
versions = ["lts", "1"]
runner = "windows-latest"

[QA]
versions = ["1"]
